begin AMP
begin
  remote = devinput
  button = KEY_VOLUMEUP
  prog   = txir
  repeat = 1
  config = IR 7A1A 255
  flags  = quit
end
begin
  remote = devinput
  button = KEY_VOLUMEDOWN
  prog   = txir
  repeat = 1
  config = IR 7A1B 255
  flags  = quit
end
begin
  remote = devinput
  button = KEY_MUTE
  prog   = txir
  repeat = 0
  config = IR 7A1C 1
  flags  = quit
end
begin
  remote = devinput
  button = KEY_ZOOM
  prog   = txir
  repeat = 0
  config = IR 7A1E 1\nLGE 0D00\nLGE 0400
  flags  = mode quit
end
end AMP
begin
  remote = devinput
  button = KEY_VOLUMEUP
  prog   = txir
  repeat = 1
  config = LGE 1B02
end
begin
  remote = devinput
  button = KEY_VOLUMEDOWN
  prog   = txir
  repeat = 1
  config = LGE 1B03
end
begin
  remote = devinput
  button = KEY_MUTE
  prog   = txir
  repeat = 0
  config = LGE 1B09
end
begin
  remote = devinput
  button = KEY_ZOOM
  prog   = txir
  repeat = 0
  config = IR 7A1D 1\nLGE 0D01\nLGE 1D22\nLGE 0401\nSLEEP 10\nIR 7AC1 1
  mode   = AMP
end
begin
  remote = devinput
  button = KEY_POWER
  prog   = txir
  repeat = 0
  config = IR 7A1E 1\nLGE 0100
end
begin
  remote = devinput
  button = KEY_POWER
  prog   = sh
  repeat = 0
  config = sleep 3;systemctl poweroff
end
