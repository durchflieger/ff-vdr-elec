begin
  remote = devinput
  button = KEY_VOLUMEUP
  prog   = txir
  repeat = 1
  config = LGE 1B02
end
begin
  remote = devinput
  button = KEY_VOLUMEDOWN
  prog   = txir
  repeat = 1
  config = LGE 1B03
end
begin
  remote = devinput
  button = KEY_MUTE
  prog   = txir
  repeat = 0
  config = LGE 1B09
end
begin
  remote = devinput
  button = KEY_POWER
  prog   = txir
  repeat = 0
  config = LGE 0100
end
begin
  remote = devinput
  button = KEY_POWER
  prog   = sh
  repeat = 0
  config = sleep 3;systemctl poweroff
end
